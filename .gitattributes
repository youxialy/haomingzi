# ============================================================
# 统一行尾 —— 仓库与工作区都存 LF，不再依赖各机器的 core.autocrlf
#
# 背景：此前没有本文件，行尾行为完全由全局 gitconfig 决定，
#       不同机器 / 不同克隆结果不一致，曾反复导致
#       「工作区 CRLF vs 线上 LF」的字节比对误报。
# ============================================================

# 默认：文本文件在仓库与工作区统一 LF（检出不做转换）
* text=auto eol=lf

# Windows 批处理必须 CRLF，否则 cmd 解析可能出错
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制：不做任何行尾处理
*.png binary
*.ico binary
*.jpg binary
*.woff binary
*.woff2 binary
